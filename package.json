{
  "name": "ivy-dev-site",
  "version": "1.0.0",
  "description": "Ivy Development Team site",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "wintersmith preview",
    "build": "wintersmith build",
    "deploy": "sh scripts/deploy.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pug": "^2.0.0-rc.4",
    "wintersmith-jade": "^0.1.0"
  },
  "devDependencies": {
    "wintersmith-livereload": "^1.0.0"
  }
}
